RewriteEngine On

# Redirect /index.php to /
RewriteCond %{THE_REQUEST} ^[A-Z]{3,}\s/index\.php[\s?]
RewriteRule ^index\.php$ / [L,R=301]

# Redirect .php extension to clean URL (GET only)
RewriteCond %{THE_REQUEST} ^GET\s(.+)\.php[\s?]
RewriteCond %{REQUEST_URI} !^/index\.php
RewriteCond %{REQUEST_URI} !^/preview_surat\.php
RewriteRule ^(.+)\.php$ /$1 [L,R=301]

# Serve clean URLs from .php files
RewriteCond %{REQUEST_URI} !\.\w+$
RewriteCond %{DOCUMENT_ROOT}%{REQUEST_URI}.php -f
RewriteRule ^(.+)$ $1.php [L]

# Protect sensitive files
<FilesMatch "^(koneksi\.php|functions\.php|install\.php|reset_password\.php)$">
    <IfModule mod_authz_core.c>
        Require all denied
    </IfModule>
    <IfModule !mod_authz_core.c>
        Deny from all
    </IfModule>
</FilesMatch>

# Block temp update directory
RewriteRule ^tmp_update(/|$) - [F,L]

Options -Indexes
ServerSignature Off

<FilesMatch "\.(sql|log|env|ini)$">
    <IfModule mod_authz_core.c>
        Require all denied
    </IfModule>
    <IfModule !mod_authz_core.c>
        Deny from all
    </IfModule>
</FilesMatch>
<FilesMatch "^\.ht">
    Require all denied
</FilesMatch>
